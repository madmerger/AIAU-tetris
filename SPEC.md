# Windows ネイティブ テトリス 仕様書（v1.0）

## 1. 目的・スコープ
既存の Unity/WebGL 版テトリス（madmerger/Tetris）と同等の機能を、Windows ネイティブアプリとしてゼロから実装する。Unity 資産は流用せず、ゲームロジック・描画・音声をすべて新規に実装する。対象は Windows 10/11 (x64) のデスクトップ単体アプリ、オフライン動作、シングルプレイのみ。

## 2. 技術構成
- 言語/実行環境: C# / .NET 8（Windows デスクトップ）
- UI・描画: WPF。盤面は `Canvas` もしくは 1 枚の描画サーフェスに毎フレーム描画（60FPS 目標、`CompositionTarget.Rendering` 駆動）
- 音声: NAudio（BGM ループ + 効果音の同時再生）
- 配布: `dotnet publish -r win-x64`（self-contained, single file）。インストーラは任意
- 構成: `Core`（盤面・ピース・スコアのロジック、UI 非依存でユニットテスト可能）/ `App`（WPF 画面・入力・音声）の 2 プロジェクト

## 3. ゲーム仕様
- 盤面: 10 列 × 20 行。ピースは 7 種（I, J, L, O, S, Z, T）で、各々固定色。
- 出現順: 7 種を 1 巡ぶんの「バッグ」からランダムに引き、1 巡使い切ったら再抽選（同一巡内での重複なし）。
- 回転: 回転中心を軸に 90 度単位。回転結果が壁・既存ブロックと衝突する場合は回転を取り消す（壁蹴りなし）。
- 落下: 一定間隔で 1 マス自動落下。着地して移動不能になった時点で固定し、ライン判定へ。
- 操作（キーボード）:
  | キー | 動作 |
  | --- | --- |
  | ← / → | 左右移動（長押しリピート） |
  | ↑ | 右回転 |
  | ↓ | ソフトドロップ（現在の落下間隔の 1/10、最小 10ms） |
  | Space | ハードドロップ（ゴースト位置へ即着地し、猶予なくその場で固定） |
  | P | ポーズ / 再開 |
- ゴーストピース: 現在ピースの着地位置を半透明で常時表示。移動・回転に追従する。
- ネクスト表示: 次のピース 1 個を盤面右上のプレビュー枠に表示。
- ライン消去: 横 1 列が埋まったら消去。消去は消えるブロックのフェードアウト演出（約 0.3 秒）を挟み、その後上の行を下へ詰める。同時に複数行消去可。演出中およびポーズ中の入力は破棄する（キューしない）。
- スコア: 同時消去 1/2/3/4 行 = `40 / 100 / 300 / 1200` × 現在レベルを加算。ハードドロップによる加点は落下距離に応じた微加点とする。
- レベル: 初期値 1。累計消去ライン 20 行ごとにレベル +1。レベルが上がるたび落下間隔を乗算で累積短縮する（`間隔 = max(0.8 × 0.9^(レベル-1), 0.05)` 秒）。
- ゲームオーバー: 新しいピースの出現位置が既に埋まっている場合。「GAME OVER」を表示し、リスタートボタンとモード選択へ戻る操作を提供。

## 4. モード
- **INFINITE MODE（インフィニティ）**: 初期盤面は空。ゲームオーバーまで継続する通常プレイ。
- **STAGE MODE（ステージ）**: 20 面の固定初期配置（あらかじめ用意した壁ブロックとジェムブロックの配置データ）を読み込む。盤面上のジェムブロックをライン消去によって全て消すとステージクリア。クリア演出（「GAME CLEAR」＋3 秒カウントダウン）後に次ステージへ自動進行し、レベル・スコアは引き継がない。最終ステージ（20 面目）クリア後は「ALL CLEAR」表示のうえモード選択画面へ戻る。壁ブロックは不滅ではなく、通常ブロックと同じくライン消去の対象に含む。ジェムが実質消去不能な配置になった詰み状態に対する自動検出・救済は行わない（通常のゲームオーバーまで継続し、リスタートで同ステージをやり直す）。ステージデータはアプリに埋め込むテキスト/CSV（`0`=空, `1`=壁ブロック, `2`=ジェム）で持ち、1 面を 20 行 × 10 列で表す。行数・列数不一致や未知の文字を含むデータは起動時に検出してエラーとして扱う。

## 5. 画面
- **モード選択画面**: タイトル、「STAGE MODE」「INFINITE MODE」の選択、ゲーム開始ボタン。選択中のモードを視覚的に区別する。
- **ゲーム画面**: 盤面、ネクストプレビュー、情報表示（経過時間 `mm:ss:ff`、レベル、消去ライン数、ステージ番号、スコア、モード名）、ポーズ/再開・リスタート・ミュート切替・モード選択へ戻るの各操作。ウィンドウはリサイズ時に盤面をアスペクト比維持でスケーリングする。

## 6. 音声
- BGM: ゲーム開始時にループ再生。ポーズ中は一時ミュート（ユーザーのミュート設定とは別管理し、ポーズ解除時はユーザー設定に戻す）。ゲームオーバー・クリア時に停止。
- 効果音: メニュー移動、ゲーム開始、ピース固定、ステージクリア。
- ミュートボタンで BGM・効果音を一括切替し、設定はアプリ終了後も保持する（ユーザー設定ファイル）。

## 7. 非機能要件
- 60FPS 相当の描画で入力遅延が体感されないこと。長時間プレイでもメモリが増加し続けないこと（ブロック要素の生成・破棄をプールで管理）。
- ゲームロジックは実時間から切り離した更新関数として実装し、盤面判定・スコア・ライン消去・ステージクリア判定をユニットテストで検証する。
- 起動 2 秒以内、.NET ランタイム未導入の環境でも single-file 配布で動作すること。

## 8. 対象外
オンライン対戦・ランキング送信、ホールド機能、SRS 準拠の壁蹴り、ゲームパッド対応、日本語以外のローカライズ、モバイル/Web 版。
